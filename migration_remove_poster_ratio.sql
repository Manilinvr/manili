-- Убирает колонку poster_aspect_ratio из site_content.
--
-- Раньше формат афиш в карусели на главной был настраиваемым (портрет /
-- квадрат / альбомный) через админку → «Контент сайта». Теперь формат
-- единый — вертикальная капсула 9:16, зашитая прямо в вёрстку сайта
-- (styles.css), поэтому ни админ-настройка, ни колонка в базе больше не
-- нужны: код нигде их не читает и не пишет.
--
-- Ничего, кроме этой одной колонки, не удаляет. Сами события, афиши и
-- остальной контент не затрагиваются. Безопасно выполнять повторно.

alter table public.site_content
  drop column if exists poster_aspect_ratio;
